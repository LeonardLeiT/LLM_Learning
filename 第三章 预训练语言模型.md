## 思维导图
![画板](https://cdn.nlark.com/yuque/0/2026/jpeg/69406261/1782570801676-f125f60c-8327-4323-a719-e1c4d63ca352.jpeg)

## 3.1 Encoder-only PLM 
** Transformer** 结构主要 由** Encoder、Decoder **两个部分组成，两个部分分别具有不⼀样的结构和输⼊输出   

> +  Google 仅选择了 Encoder 层，BERT。
> + OpenAI 选择了 Decoder 层，GPT 系列模型
> + 同时保留 Encoder 与 Decoder，打造预训练的 Transformer 模型，例如由 Google 发布的 T5模型  
>

### 3.1.1 BERT 
BERT，全名为 Bidirectional Encoder Representations from Transformers，是由 Google 团队在 2018年发布的 预训练语⾔模型

####  （1）思想沿承  
> +  Transformer 架构。完全使⽤注意⼒机制⽽抛弃 RNN、LSTM 结构的 Transformer 模型，带来了新的模型架构。BERT 正沿承 了 Transformer 的思想，在 Transformer 的模型基座上进⾏优化，通过将 Encoder 结构进⾏堆叠，扩⼤模型参数，打造了在 NLU 任务上独居天分的模型架构；
> + 预训练+微调范式。ELMo 模型基于双向 LSTM 架构，在训练数据上基于语⾔模型进⾏预训练，再针对下游任务进⾏微调，表现出了更加优越的性能， 将 NLP 领域导向预训练+微调的研究思路。⽽ BERT 也采⽤了该范式，并通过将模型架构调整为 Transformer，引⼊更适合⽂本理解、能捕捉深层双向语义关系的预训练任务 MLM，将预训练-微调范式推向了⾼潮。  
>

####  （2）模型架构——Encoder Only  
<img src="https://raw.githubusercontent.com/datawhalechina/happy-llm/main/docs/images/3-figures/1-0.png" width="1244" title="BERT 模型结构" crop="0,0,1,1" id="UtnP5" class="ne-image">

 BERT 本质上也是⼀ 个 Seq2Seq 模型，只是没有加⼊对特定任务的 Decoder，因此，为适配各种 NLU 任务，在模型的最顶层加⼊了⼀ 个分类头 prediction_heads，⽤于将多维度的隐藏状态通过线性层转换到分类维度  



<font style="color:#000000;">通过 tokenizer（分词器） 转化成 input_ids（基本每一个模型在 tokenizer 的操作都类似，可以参考 Transformer 的 tokenizer 机制，后文不再赘述），然后进入 Embedding 层转化为特定维度的 hidden_states，再经过 Encoder 块</font>

:::info
**<font style="color:rgb(133, 133, 133);">BERT 采用 WordPiece 作为分词方法。WordPiece 是一种基于统计的子词切分算法，其核心在于将单词拆解为子词（例如，"playing" -> ["play", "##ing"]）。其合并操作的依据是最大化语言模型的似然度。对于中文等非空格分隔的语言，通常将单个汉字作为原子分词单位（token）处理。</font>**

:::

**<font style="color:#000000;">激活函数是 GELU 函数</font>**

**<font style="color:#000000;"></font>**

$  \text{GELU}(x) = 0.5x \left(1 + \tanh\left(\sqrt{\frac{2}{\pi}} \left(x + 0.044715x^3\right)\right)\right)  $

####  （3）预训练任务——MLM + NSP  
 相较于基本沿承 Transformer 的模型架构，BERT 更⼤的创新点在于其提出的两个新的预训练任务上——MLM 和 NSP（Next Sentence Prediction，下⼀句预测）  



####  （4）下游任务微调  
 作为 NLP 领域⾥程碑式的成果，BERT 的⼀个重⼤意义就是正式确⽴了预训练-微调的两阶段思想，即在海量⽆监 督语料上进⾏预训练来获得通⽤的⽂本理解与⽣成能⼒，再在对应的下游任务上进⾏微调。该种思想的⼀个重点在 于，预训练得到的强⼤能⼒能否通过低成本的微调快速迁移到对应的下游任务上。  

###  3.1.2 RoBERTa  
作为 NLP 领域⾥程碑式的成果，BERT 的⼀个重⼤意义就是正式确⽴了预训练-微调的两阶段思想，即在海量⽆监 督语料上进⾏预训练来获得通⽤的⽂本理解与⽣成能⼒，再在对应的下游任务上进⾏微调。该种思想的⼀个重点在 于，预训练得到的强⼤能⼒能否通过低成本的微调快速迁移到对应的下游任务上。  

>  但是，13GB 的预训练数据是否让 BERT 达到了充分的拟合呢？如果我们使⽤更多预训练语料，是否可以进⼀步增 强模型性能？更多的，BERT 所选⽤的预训练任务、训练超参数是否是最优的？RoBERTa 应运⽽⽣。  
>

####  （1）优化⼀：去掉 NSP 预训练任务  
<font style="color:#000000;"> RoBERTa 设置了四个实验组： </font>

> 1. 段落构建的 MLM + NSP：BERT 原始预训练任务，输⼊是⼀对⽚段，每个⽚段包括多个句⼦，来构造 NSP 任务；
> 2. ⽂档对构建的 MLM + NSP：⼀个输⼊构建⼀对句⼦，通过增⼤ batch 来和原始输⼊达到 token 等同；
> 3. 跨越⽂档的 MLM：去掉 NSP 任务，⼀个输⼊为从⼀个或多个⽂档中连续采样的完整句⼦，为使输⼊达到最⼤⻓度（512），可能⼀个输⼊会包括多个⽂档；
> 4. 单⽂档的 MLM：去掉 NSP 任务，且限制⼀个输⼊只能从⼀个⽂档中采样，同样通过增⼤ batch 来和原始输⼊达到token 等同
>

 实验结果证明，**后两组显著优于前两组**，且单⽂档的 MLM 组在下游任务上微调时性能最佳。因此，RoBERTa 在预训练中**去掉了 NSP**，只使⽤ MLM 任务。

####  （2）优化⼆：更⼤规模的预训练数据和预训练步⻓  
 RoBERTa 使⽤了更⼤量的⽆监督语料进⾏预训练 

 RoBERTa 认为更⼤的 batch size 既可以提⾼优化速度，也可以提⾼任务结束性能。  

####  （3）优化三：更⼤的 bpe 词表  
 与 BERT 使⽤的 WordPiece 算法不同，RoBERTa 使⽤了 BPE 作为 Tokenizer 的编码策略。BPE，即 Byte Pair Encoding，字节对编码，是指以⼦词对作为分词的单位。例如，对“Hello World”这句话，可能会切分为“Hel，lo， Wor，ld”四个⼦词对。⽽对于以字为基本单位的中⽂，⼀般会按照字节编码进⾏切分。例如，在 UTF-8 编码中， “我”会被编码为“E68891”，那么在 BPE 中可能就会切分成“E68”，“891”两个字词对。  

###  3.1.3 ALBERT 
 在 BERT 的基础上，RoBERTa 进⼀步探究了更⼤规模预训练的作⽤。同样是基于 BERT 架构进⾏优化的 ALBERT 模 型，则从是否能够减⼩模型参数保持模型能⼒的⻆度展开了探究。  

####  （1）优化⼀：将 Embedding 参数进⾏分解  
 ALBERT 对 Embedding 层的参数矩阵进⾏了分解，让 Embedding 层的输出维度和隐藏层维度解绑，也就 是在 Embedding 层的后⾯加⼊⼀个线性矩阵进⾏维度变换。ALBERT 设置了 Embedding 层的输出为 128，因此 在 Embedding 层后⾯加⼊了⼀个 的线性矩阵来将 Embedding 层的输出再升维到隐藏层⼤⼩。 

####  （2）优化⼆：跨层进⾏参数共享  
 ALBERT 发现各个 Encoder 层的参数出现⾼度⼀致的情况。由于 24个 Encoder 层 带来了巨⼤的模型参数，因此，ALBERT 提出，可以让各个 Encoder 层共享模型参数，来减少模型的参数量  

####  （3）优化三：提出 SOP 预训练任务  
 类似于 RoBERTa，ALBERT 也同样认为 NSP 任务过于简单，在预训练中⽆法对模型效果的提升带来显著影响。但 是不同于 RoBERTa 选择直接去掉 NSP，ALBERT 选择改进 NSP，增加其难度，来优化模型的预训练。  
